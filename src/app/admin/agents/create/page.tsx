import HeaderText from "@/components/HeaderText";
import { Box } from "@mui/material";
import React from "react";

const Agents = () => {
  return (
    <Box
      sx={{
        padding: "2rem",
      }}
    >
      <HeaderText heading="Add Agent" />
    </Box>
  );
};

export default Agents;
